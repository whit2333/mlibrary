from init_env import init_environment

env = init_environment("mlibrary qt5")

sources = Split("""
	gVolume.cc
	utilities.cc
	gSystem.cc
	systemFactories/registerFactory.cc
	systemFactories/systemTextFactory.cc
	""")

lib = env.Library(source = sources, target = "../lib/gVolume")
