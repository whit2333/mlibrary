from init_env import init_environment

env = init_environment("mlibrary qt5 geant4 clhep")

txtSources = Split("""
	factories/txt/publishHeader.cc
	""")

txt_dll  = env.SharedLibrary( source = txtSources,  target = "txtGMedia")


