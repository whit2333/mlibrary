// gmedia
#include "gmediaROOTFactory.h"

bool GMediaROOTFactory::publishDetectorTrueObservables(GDetectorObservables *detectorHits)
{
	if(rootfile == nullptr) return false;
	
	// get root tree from the map,
	
	return true;
	
}
